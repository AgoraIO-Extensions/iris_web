{
  "name": "@iris/native-rtc-binding",
  "version": "1.0.0",
  "description": "TypeScript definitions for native rtc",
  "types": "./index.ts",
  "scripts": {
    "build": "tsup",
    "lint": "eslint --ext .ts generate",
    "lint:fix": "eslint --fix --ext .ts ./generate"
  },
  "dependencies": {
    "iris-web-core": "workspace:*"
  }
}
